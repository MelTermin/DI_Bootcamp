// -- 	SELECT COUNT (*) FROM actors
	 
// -- INSERT INTO actors (first_name, last_name, age, number_oscars)
// -- VALUES ('Britney','','1970-10-08', 5); => even though I added constraints of NOT NULL it added to the table but with a blank row

// select * from actors